<div class="desoc-container badges-grid">
  {{#if credentials}}
    <div class="desoc-group-list">
      {{#each credentials as |credential|}}
        <BadgeItem @badge={{credential}} />
      {{/each}}
    </div>
  {{/if}}
</div>